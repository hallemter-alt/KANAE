# Git 版本管理與發布流程指南

## 📋 目錄

1. [版本命名規範](#版本命名規範)
2. [發布流程](#發布流程)
3. [Git 標籤管理](#git-標籤管理)
4. [GitHub Release 創建](#github-release-創建)
5. [安全檢查清單](#安全檢查清單)
6. [常見問題](#常見問題)

---

## 版本命名規範

我們採用 **語義化版本控制 (Semantic Versioning)**，格式為：`v主版本.次版本.修訂號`

```
v主版本.次版本.修訂號
 │      │      └─ 修復 bug 或小改動時遞增
 │      └────────── 新增功能但向後兼容時遞增
 └───────────────── 重大變更、不向後兼容時遞增
```

### 版本類型說明

- **v0.x.x** - 初始開發階段（當前階段）
  - `v0.1.0` - MVP 完成（初始版本）✅ 當前版本
  - `v0.2.0` - 添加物件搜索功能
  - `v0.3.0` - 添加物件詳情頁
  - `v0.9.0` - Beta 測試版本

- **v1.x.x** - 穩定版本（正式發布）
  - `v1.0.0` - 第一個正式發布版本
  - `v1.1.0` - 新增功能
  - `v1.1.1` - Bug 修復

- **v2.x.x** - 重大更新
  - `v2.0.0` - 架構重大變更或破壞性更新

### 版本號遞增規則

| 變更類型 | 範例 | 版本遞增 |
|---------|------|---------|
| 🐛 Bug 修復 | 修復導航欄在移動端的顯示問題 | `v0.1.0` → `v0.1.1` |
| ✨ 新功能 | 添加物件搜索功能 | `v0.1.0` → `v0.2.0` |
| 💥 破壞性變更 | 重構整個路由系統 | `v0.9.0` → `v1.0.0` |
| 📝 文檔更新 | 更新 README | 不變更版本 |
| 🎨 樣式調整 | 微調顏色、間距 | `v0.1.0` → `v0.1.1` |

---

## 發布流程

### 方式一：完整的發布流程（推薦）

#### Step 1: 確保代碼品質

```bash
# 1. 確保在 main 分支
cd /home/user/webapp
git checkout main

# 2. 拉取最新代碼
git pull origin main

# 3. 確保所有測試通過
npm run lint
npm run build

# 4. 檢查是否有未提交的更改
git status
```

#### Step 2: 更新版本號

```bash
# 手動更新 package.json 中的 version 欄位
# 從 "0.1.0" 改為 "0.2.0"

# 提交版本號更改
git add package.json
git commit -m "chore: Bump version to 0.2.0"
```

#### Step 3: 創建 Git 標籤

```bash
# 創建帶註釋的標籤（推薦）
git tag -a v0.2.0 -m "Release v0.2.0 - 物件搜索功能

✨ 新功能
- 賃貸物件搜索頁面
- 高級篩選功能（地區、價格、間取り）
- 地圖整合顯示
- 收藏功能

🐛 Bug 修復
- 修復導航欄在移動端的顯示問題
- 優化圖片加載速度

📝 改進
- 優化 SEO 配置
- 提升移動端體驗
"
```

#### Step 4: 推送到 GitHub

```bash
# 推送代碼和標籤
git push origin main
git push origin v0.2.0

# 或者一次推送所有標籤
git push origin main --tags
```

#### Step 5: 在 GitHub 上創建 Release

1. 訪問 GitHub 儲存庫頁面
2. 點擊右側 "Releases" 或訪問 `https://github.com/你的用戶名/webapp/releases`
3. 點擊 "Draft a new release"
4. 選擇剛才創建的標籤 `v0.2.0`
5. 填寫 Release 標題：`v0.2.0 - 物件搜索功能`
6. 填寫發布說明（使用下方模板）
7. 如果是預發布版本，勾選 "This is a pre-release"
8. 點擊 "Publish release"

### 方式二：快速發布流程（小版本）

```bash
# 對於小的修復或更新
git tag -a v0.1.1 -m "Release v0.1.1 - Bug fixes"
git push origin main --tags
```

---

## Git 標籤管理

### 查看所有標籤

```bash
# 列出所有標籤
git tag

# 列出標籤及其說明
git tag -n

# 查看特定標籤的詳細信息
git show v0.1.0
```

### 刪除標籤

```bash
# 刪除本地標籤
git tag -d v0.1.0

# 刪除遠程標籤
git push origin :refs/tags/v0.1.0
# 或
git push origin --delete v0.1.0
```

### 修改標籤

```bash
# 標籤一旦創建就不應該修改！
# 如果必須修改，需要刪除後重新創建

# 1. 刪除本地標籤
git tag -d v0.1.0

# 2. 刪除遠程標籤
git push origin :refs/tags/v0.1.0

# 3. 重新創建標籤
git tag -a v0.1.0 -m "新的說明"

# 4. 強制推送（注意：這會影響其他開發者）
git push origin v0.1.0 --force
```

---

## GitHub Release 創建

### Release 說明模板

```markdown
## v0.2.0 - 物件搜索功能

**發布日期**: 2026-01-13

### ✨ 新功能

- 賃貸物件搜索頁面 (`/rent/search`)
  - 地區篩選（東京23區、神奈川縣等）
  - 價格範圍篩選
  - 間取り選擇（1K, 1LDK, 2LDK等）
  - 車站步行時間篩選
- 地圖整合顯示
  - Google Maps 整合
  - 物件位置標記
  - 周邊設施顯示
- 收藏功能
  - 登入用戶可收藏物件
  - 收藏列表管理

### 🐛 Bug 修復

- 修復導航欄在 iOS Safari 中的顯示問題 (#12)
- 修復圖片在慢速網絡下的加載問題 (#15)
- 修正多語言切換時的狀態保持問題 (#18)

### 🔧 改進

- 優化首次加載速度（提升 30%）
- 改善移動端觸控體驗
- 更新 SEO metadata 配置
- 添加 Google Analytics 整合

### 📝 文檔

- 更新 README 添加搜索功能說明
- 添加 API 文檔（`/docs/api.md`）
- 更新部署指南

### 🔄 依賴更新

- Next.js 15.5.9 → 15.6.0
- React 19.0.0 → 19.0.1
- Tailwind CSS 3.4.17 → 3.4.18

### ⚠️ 破壞性變更

無

### 📦 資源

- **演示網站**: https://kanae-webapp.vercel.app
- **文檔**: https://docs.kanae-webapp.com
- **變更日誌**: [CHANGELOG.md](./CHANGELOG.md)

### 🙏 致謝

感謝所有貢獻者和測試人員！

---

**完整變更列表**: [v0.1.0...v0.2.0](https://github.com/你的用戶名/webapp/compare/v0.1.0...v0.2.0)
```

### GitHub Release 類型

| 類型 | 何時使用 | GitHub 設定 |
|------|---------|------------|
| Latest Release | 正式穩定版本 | 不勾選 "pre-release" |
| Pre-release | Beta 測試版 | 勾選 "This is a pre-release" |
| Draft | 準備中的發布 | 保存為草稿 |

---

## 安全檢查清單

### 發布前必須檢查

```bash
# ✅ 1. 檢查是否有機密文件被追蹤
git ls-files | grep -iE "\.(env|key|pem|secret)"

# ✅ 2. 檢查 .gitignore 是否正確
cat .gitignore | grep -E "(\.env|secret|key|password)"

# ✅ 3. 檢查提交歷史中是否有敏感信息
git log --all --full-history -- "*.env" "*.key" "*.pem"

# ✅ 4. 搜索代碼中的硬編碼密鑰
git grep -iE "(api.?key|password|secret.*=)" 

# ✅ 5. 確認沒有未提交的更改
git status

# ✅ 6. 確認構建成功
npm run build
```

### 如果發現機密信息已被提交

#### 方法一：使用 git filter-branch（小型倉庫）

```bash
# ⚠️ 警告：這會重寫歷史！所有開發者需要重新克隆

# 從所有歷史中刪除特定文件
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch .env" \
  --prune-empty --tag-name-filter cat -- --all

# 強制推送（需要通知所有協作者）
git push origin --force --all
git push origin --force --tags
```

#### 方法二：使用 BFG Repo-Cleaner（推薦）

```bash
# 1. 下載 BFG
wget https://repo1.maven.org/maven2/com/madgag/bfg/1.14.0/bfg-1.14.0.jar

# 2. 刪除特定文件
java -jar bfg-1.14.0.jar --delete-files .env

# 3. 清理
git reflog expire --expire=now --all
git gc --prune=now --aggressive

# 4. 強制推送
git push origin --force --all
```

#### 方法三：GitHub Secret Scanning（自動化）

- GitHub 會自動掃描公開儲存庫中的密鑰
- 如果檢測到，會發送警報通知
- 立即輪換（更換）被洩露的密鑰

---

## 常見問題

### Q1: 如何創建 v0.2.0 版本？

```bash
# 1. 完成新功能開發並提交
git add .
git commit -m "feat: Add property search functionality"

# 2. 創建標籤
git tag -a v0.2.0 -m "Release v0.2.0 - Property Search Feature

✨ New Features
- Property search page
- Advanced filtering
- Map integration
"

# 3. 推送
git push origin main --tags
```

### Q2: 如何創建 v0.1.1（修復版本）？

```bash
# 1. 修復 bug 並提交
git add .
git commit -m "fix: Resolve navbar display issue on mobile"

# 2. 創建標籤
git tag -a v0.1.1 -m "Release v0.1.1 - Bug Fixes

🐛 Bug Fixes
- Fix navbar display issue on mobile
- Improve image loading performance
"

# 3. 推送
git push origin main --tags
```

### Q3: 如何創建 v1.0.0（正式版）？

```bash
# 當所有核心功能完成並穩定後

# 1. 更新版本號
# 在 package.json 中更改為 "1.0.0"

# 2. 提交版本更新
git add package.json
git commit -m "chore: Release v1.0.0 - Stable Release"

# 3. 創建標籤
git tag -a v1.0.0 -m "Release v1.0.0 - First Stable Release 🎉

🎉 正式發布！

這是 KANAE 不動產網站的第一個穩定版本。

完整功能：
- 響應式首頁
- 物件搜索（賃貸/売買）
- 物件詳情頁
- 民泊業務頁
- 多語言支持（日中英）
- 企業理念展示
- 聯繫表單
- SEO 優化

技術亮點：
- Next.js 15 (App Router)
- TypeScript
- Tailwind CSS
- 完整的安全配置

感謝所有貢獻者！
"

# 4. 推送
git push origin main --tags

# 5. 在 GitHub 創建重要的 Release（標記為 "Latest Release"）
```

### Q4: 標籤與 Release 有什麼區別？

| 項目 | Git 標籤 (Tag) | GitHub Release |
|------|---------------|----------------|
| 定義 | Git 的輕量級引用，指向特定提交 | GitHub 的功能，基於標籤創建 |
| 位置 | 存在於 Git 倉庫中 | 存在於 GitHub 網站上 |
| 內容 | 標籤名稱 + 簡短說明 | 標籤 + 詳細說明 + 附件 + 下載連結 |
| 創建 | `git tag` | GitHub 網站或 GitHub CLI |
| 用途 | 版本控制、代碼回溯 | 發布通知、提供下載、展示變更 |

**推薦做法**：
1. 先創建 Git 標籤（`git tag`）
2. 推送到 GitHub（`git push --tags`）
3. 基於標籤創建 GitHub Release（在網站上操作）

### Q5: 如何查看兩個版本之間的差異？

```bash
# 查看兩個標籤之間的所有提交
git log v0.1.0..v0.2.0 --oneline

# 查看詳細差異
git diff v0.1.0 v0.2.0

# 查看變更的文件列表
git diff v0.1.0 v0.2.0 --name-only

# 查看統計信息
git diff v0.1.0 v0.2.0 --stat
```

### Q6: 發布後發現重大 bug 怎麼辦？

```bash
# 方法一：緊急修復版本（推薦）
# 1. 修復 bug
git add .
git commit -m "fix: Critical bug fix for release v0.2.0"

# 2. 創建修復版本
git tag -a v0.2.1 -m "Release v0.2.1 - Critical Bug Fix"
git push origin main --tags

# 方法二：撤回發布（不推薦，除非絕對必要）
# 在 GitHub 上將 Release 標記為 "Pre-release"
# 或刪除 Release（不刪除標籤）
```

---

## 快速參考

### 常用命令速查表

```bash
# 查看所有標籤
git tag

# 創建標籤
git tag -a v0.2.0 -m "Release notes"

# 推送標籤
git push origin v0.2.0
git push origin --tags  # 推送所有標籤

# 刪除標籤
git tag -d v0.2.0                    # 本地刪除
git push origin --delete v0.2.0      # 遠程刪除

# 查看標籤詳情
git show v0.2.0

# 檢出標籤
git checkout v0.2.0

# 查看版本差異
git diff v0.1.0 v0.2.0
```

### 版本規劃建議

| 版本 | 內容 | 預計時間 |
|------|------|---------|
| v0.1.0 | MVP 完成（首頁） | ✅ 已完成 |
| v0.2.0 | 物件搜索功能 | 2週 |
| v0.3.0 | 物件詳情頁 | 2週 |
| v0.4.0 | 民泊業務頁 | 1週 |
| v0.5.0 | 用戶系統 | 3週 |
| v0.6.0 | 後台管理 | 4週 |
| v0.9.0 | Beta 測試 | 2週 |
| v1.0.0 | 正式發布 | - |

---

## 附錄

### Commit 訊息規範

遵循 [Conventional Commits](https://www.conventionalcommits.org/) 規範：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 類型**：
- `feat`: 新功能
- `fix`: Bug 修復
- `docs`: 文檔更新
- `style`: 代碼格式（不影響功能）
- `refactor`: 重構
- `perf`: 性能優化
- `test`: 測試相關
- `chore`: 構建/工具變動
- `security`: 安全相關

**範例**：
```bash
git commit -m "feat(search): Add property search with advanced filters"
git commit -m "fix(navbar): Resolve display issue on iOS Safari"
git commit -m "docs: Update README with API documentation"
git commit -m "security: Update dependencies to patch vulnerabilities"
```

### 相關資源

- [語義化版本控制](https://semver.org/lang/zh-TW/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [GitHub Releases 文檔](https://docs.github.com/en/repositories/releasing-projects-on-github)
- [Git 標籤文檔](https://git-scm.com/book/zh-tw/v2/Git-基礎-標籤)

---

**更新日期**: 2026-01-12  
**版本**: 1.0  
**維護者**: KANAE Development Team
