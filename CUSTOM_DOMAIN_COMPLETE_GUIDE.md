# 🌐 Cloudflare Pages 自定義域名綁定 - 完整實戰指南

## 📋 任務目標

將您的正式域名（例如：yourdomain.com）綁定到 Cloudflare Pages 專案（kanae-real-estate.pages.dev）。

---

## 前提條件

- ✅ 已擁有 Cloudflare 帳號
- ✅ 已購買域名（在域名註冊商處）
- ✅ 已在 Cloudflare Pages 部署專案
- ✅ 具有域名管理權限

---

# Part 1: 在 Cloudflare 中添加新域名

## 步驟 1.1：登入 Cloudflare Dashboard

1. **打開瀏覽器**，前往：
   ```
   🔗 https://dash.cloudflare.com/
   ```

2. **登入您的 Cloudflare 帳號**
   - 輸入郵箱和密碼
   - 點擊 "Log in"

---

## 步驟 1.2：添加新站點（Add a Site）

1. **在主控制台**，找到左側導航欄

2. **點擊 "Websites"**（網站）選項

3. **點擊右上角的 "Add a Site"** 按鈕

```
┌─────────────────────────────────────────┐
│  Cloudflare Dashboard                   │
│  ┌───────────────────────────────────┐  │
│  │  🌐 Websites    [+ Add a Site]   │  │ ← 點擊這裡
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

---

## 步驟 1.3：輸入域名

1. **在彈出的輸入框中**，輸入您的域名

```
┌─────────────────────────────────────────┐
│  Add a site to Cloudflare              │
│  ┌───────────────────────────────────┐ │
│  │  Enter your site (example.com)    │ │
│  │  [yourdomain.com              ]   │ │ ← 輸入域名
│  └───────────────────────────────────┘ │
│                                         │
│  [Continue]                             │
└─────────────────────────────────────────┘
```

**輸入規則**：
- ✅ 只輸入域名本身（例如：kanae-estate.com）
- ❌ 不要包含 `http://` 或 `https://`
- ❌ 不要包含 `www`
- ❌ 不要包含尾隨斜線 `/`

**示例**：
```
✅ 正確：kanae-estate.com
❌ 錯誤：https://kanae-estate.com
❌ 錯誤：www.kanae-estate.com
❌ 錯誤：kanae-estate.com/
```

2. **點擊 "Continue"**（繼續）按鈕

---

## 步驟 1.4：選擇方案

Cloudflare 會顯示可用的方案：

```
┌─────────────────────────────────────────┐
│  Select a plan                          │
│  ┌─────────────────────────────────┐   │
│  │  ○ Free        $0/month         │   │ ← 選擇這個
│  │  ○ Pro         $20/month        │   │
│  │  ○ Business    $200/month       │   │
│  │  ○ Enterprise  Custom pricing   │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [Continue]                             │
└─────────────────────────────────────────┘
```

1. **選擇 "Free" 方案**（免費方案已足夠）
   - 包含 CDN、SSL、DDoS 防護
   - 對一般網站完全足夠

2. **點擊 "Continue"**

---

## 步驟 1.5：DNS 記錄掃描

Cloudflare 會自動掃描您域名的現有 DNS 記錄：

```
┌─────────────────────────────────────────┐
│  Review DNS records                     │
│  ┌───────────────────────────────────┐ │
│  │  Scanning DNS records...          │ │
│  │  Found 5 records                  │ │
│  │                                   │ │
│  │  Type   Name      Target          │ │
│  │  A      @         192.0.2.1       │ │
│  │  CNAME  www       example.com     │ │
│  │  MX     @         mail.example... │ │
│  │  ...                              │ │
│  └───────────────────────────────────┘ │
│                                         │
│  [Continue]                             │
└─────────────────────────────────────────┘
```

1. **檢查掃描到的 DNS 記錄**
   - Cloudflare 會顯示找到的記錄
   - 這些是您域名當前的 DNS 設定

2. **確認重要記錄都已識別**
   - 如果有遺漏，稍後可以手動添加
   - 現在先不用修改

3. **點擊 "Continue"**

---

## 步驟 1.6：更改 Nameservers（關鍵步驟）

這是**最重要**的步驟！

```
┌─────────────────────────────────────────────────┐
│  Update your nameservers                        │
│  ┌───────────────────────────────────────────┐  │
│  │  Change nameservers at your registrar to: │  │
│  │                                           │  │
│  │  📋 clara.ns.cloudflare.com               │  │ ← 複製這兩個
│  │  📋 noah.ns.cloudflare.com                │  │
│  │                                           │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  [Check nameservers]                            │
└─────────────────────────────────────────────────┘
```

### 1.6.1：記下 Nameservers

Cloudflare 會提供兩個 nameserver 地址，例如：
```
NS1: clara.ns.cloudflare.com
NS2: noah.ns.cloudflare.com
```

**重要**：您的 nameservers 可能不同，請使用 Cloudflare 顯示的地址。

### 1.6.2：前往域名註冊商

**常見域名註冊商**：
- GoDaddy
- Namecheap
- Google Domains
- Gandi
- Name.com
- 其他

**操作步驟**（以 GoDaddy 為例）：

1. **登入域名註冊商網站**

2. **找到域名管理頁面**
   - 通常叫 "Domain Management"
   - 或 "My Domains"

3. **選擇您的域名**
   - 點擊要設定的域名

4. **找到 Nameservers 設定**
   - 通常在 "DNS Settings" 或 "Nameservers" 區域
   - 可能需要點擊 "Change" 或 "Edit"

5. **選擇 "Custom Nameservers"**
   - 或 "Use custom nameservers"

6. **輸入 Cloudflare 的 Nameservers**
   ```
   Nameserver 1: clara.ns.cloudflare.com
   Nameserver 2: noah.ns.cloudflare.com
   ```

7. **保存設定**
   - 點擊 "Save" 或 "Update"

### 1.6.3：返回 Cloudflare

1. **回到 Cloudflare 頁面**

2. **點擊 "Check nameservers"**（檢查 nameservers）
   - 或 "Done, check nameservers"

---

## 步驟 1.7：等待 DNS 傳播

更改 nameservers 後：

```
┌─────────────────────────────────────────┐
│  Nameserver update in progress          │
│  ┌───────────────────────────────────┐  │
│  │  ⏳ Checking nameservers...       │  │
│  │                                   │  │
│  │  This may take up to 24 hours    │  │
│  │  We'll send you an email when     │  │
│  │  your site is active              │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

**時間預估**：
- ⏱️ **通常**：5-60 分鐘
- ⏱️ **最長**：24-48 小時
- 📧 Cloudflare 會發送郵件通知您

**期間您可以**：
- 繼續配置其他設定
- 檢查郵箱等待確認
- 使用工具檢查 DNS 傳播狀態

**檢查工具**：
```
🔗 https://dnschecker.org/
輸入您的域名，選擇 NS 記錄
查看全球各地的 DNS 服務器狀態
```

---

## 步驟 1.8：確認域名已激活

當 DNS 傳播完成：

```
✅ 您會收到郵件：
   "Your site is now active on Cloudflare"

✅ Cloudflare Dashboard 顯示：
   狀態：Active
   圖標：綠色勾選
```

**現在可以進入 Part 2 配置 Pages 專案！**

---

# Part 2: 在 Pages 專案中設定 Custom Domain

**前提**：域名已在 Cloudflare 中激活（完成 Part 1）

---

## 步驟 2.1：進入 Pages 專案

1. **在 Cloudflare Dashboard**，點擊左側的 **"Workers & Pages"**

```
┌─────────────────────────────────────┐
│  Cloudflare Dashboard               │
│  ├─ 🏠 Home                         │
│  ├─ 🌐 Websites                     │
│  ├─ ⚡ Workers & Pages  ◀────────   │ 點擊這裡
│  └─ ...                             │
└─────────────────────────────────────┘
```

2. **找到您的 Pages 專案**
   - 例如：`kanae-real-estate`

3. **點擊專案名稱**

```
┌─────────────────────────────────────────┐
│  Workers & Pages                        │
│  ┌───────────────────────────────────┐  │
│  │  📄 kanae-real-estate             │  │ ← 點擊
│  │     kanae-real-estate.pages.dev   │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

---

## 步驟 2.2：進入 Custom Domains 設定

1. **在專案頁面**，點擊頂部的 **"Custom domains"** 標籤

```
┌─────────────────────────────────────────┐
│  kanae-real-estate                      │
│  ┌───────────────────────────────────┐  │
│  │  Deployments │ Custom domains  ◀─ │  │ 點擊這個標籤
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

**如果找不到 Custom domains 標籤**：
- 點擊 **"Settings"** 標籤
- 在左側選單找到 **"Custom domains"**

---

## 步驟 2.3：添加自定義域名

1. **點擊 "Set up a custom domain"** 按鈕

```
┌─────────────────────────────────────────┐
│  Custom domains                         │
│  ┌───────────────────────────────────┐  │
│  │  No custom domains configured     │  │
│  │                                   │  │
│  │  [Set up a custom domain]  ◀───── │  │ 點擊這個
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

2. **在彈出的輸入框中**，輸入您的域名

---

## 步驟 2.4：選擇域名類型

您有兩個選擇：

### 選項 A：子域名（推薦先設定）

**輸入**：`www.yourdomain.com`

```
┌─────────────────────────────────────────┐
│  Set up a custom domain                 │
│  ┌───────────────────────────────────┐  │
│  │  Domain                           │  │
│  │  [www.kanae-estate.com        ]   │  │
│  └───────────────────────────────────┘  │
│                                         │
│  [Continue]                             │
└─────────────────────────────────────────┘
```

**優點**：
- ✅ 設定簡單
- ✅ 相容性好
- ✅ 常見用法

### 選項 B：根域名

**輸入**：`yourdomain.com`（不帶 www）

```
┌─────────────────────────────────────────┐
│  Set up a custom domain                 │
│  ┌───────────────────────────────────┐  │
│  │  Domain                           │  │
│  │  [kanae-estate.com            ]   │  │
│  └───────────────────────────────────┘  │
│                                         │
│  [Continue]                             │
└─────────────────────────────────────────┘
```

**建議**：同時設定兩者（先 www，後根域名）

3. **點擊 "Continue"**

---

## 步驟 2.5：Cloudflare 自動配置 DNS

如果域名已在 Cloudflare 管理：

```
┌─────────────────────────────────────────┐
│  Activate domain                        │
│  ┌───────────────────────────────────┐  │
│  │  ✅ DNS record will be added      │  │
│  │     automatically                 │  │
│  │                                   │  │
│  │  Type:   CNAME                    │  │
│  │  Name:   www                      │  │
│  │  Target: kanae-real-estate...     │  │
│  │  Proxy:  Proxied                  │  │
│  └───────────────────────────────────┘  │
│                                         │
│  [Activate domain]                      │
└─────────────────────────────────────────┘
```

**Cloudflare 會自動**：
- ✅ 添加所需的 DNS 記錄
- ✅ 啟用 Proxy（CDN）
- ✅ 配置 SSL 憑證

1. **點擊 "Activate domain"**（激活域名）

2. **等待幾秒鐘**，域名會被激活

---

## 步驟 2.6：重複添加其他域名（可選）

如果您想同時支持 www 和根域名：

1. **重複步驟 2.3-2.5**

2. **這次輸入另一個域名**：
   - 如果剛才設定了 `www.kanae-estate.com`
   - 現在設定 `kanae-estate.com`（或反之）

**最終結果**：

```
┌─────────────────────────────────────────┐
│  Custom domains                         │
│  ┌───────────────────────────────────┐  │
│  │  ✅ www.kanae-estate.com          │  │
│  │     Status: Active                │  │
│  │                                   │  │
│  │  ✅ kanae-estate.com              │  │
│  │     Status: Active                │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

---

# Part 3: DNS 記錄設定詳解

## 查看自動創建的 DNS 記錄

Cloudflare 自動添加域名時，會創建相應的 DNS 記錄。

### 查看步驟：

1. **在 Cloudflare Dashboard**，點擊左側的 **"Websites"**

2. **點擊您的域名**（例如：kanae-estate.com）

3. **點擊 "DNS"** 標籤

4. **點擊 "Records"**

您會看到類似這樣的記錄：

---

## DNS 記錄類型說明

### 子域名（www）的 DNS 記錄

```
┌──────────────────────────────────────────────────┐
│  DNS Records                                     │
├──────┬──────┬─────────────────────────┬─────────┤
│ Type │ Name │ Target                  │ Proxy   │
├──────┼──────┼─────────────────────────┼─────────┤
│ CNAME│ www  │ kanae-real-estate.      │Proxied☁│
│      │      │ pages.dev               │         │
└──────┴──────┴─────────────────────────┴─────────┘
```

**記錄詳解**：
- **Type**: `CNAME`
- **Name**: `www`
- **Target**: `kanae-real-estate.pages.dev`
- **Proxy status**: `Proxied`（橙色雲朵圖標）
- **TTL**: `Auto`

---

### 根域名（@）的 DNS 記錄

```
┌──────────────────────────────────────────────────┐
│  DNS Records                                     │
├──────┬──────┬─────────────────────────┬─────────┤
│ Type │ Name │ Target                  │ Proxy   │
├──────┼──────┼─────────────────────────┼─────────┤
│ CNAME│  @   │ kanae-real-estate.      │Proxied☁│
│      │      │ pages.dev               │         │
└──────┴──────┴─────────────────────────┴─────────┘
```

**記錄詳解**：
- **Type**: `CNAME`
- **Name**: `@`（代表根域名）
- **Target**: `kanae-real-estate.pages.dev`
- **Proxy status**: `Proxied`
- **TTL**: `Auto`

---

## 手動添加 DNS 記錄（如需要）

如果自動配置失敗，您可以手動添加：

### 步驟 3.1：進入 DNS 設定

1. **Cloudflare Dashboard** > **Websites** > **您的域名**
2. **點擊 "DNS"** 標籤
3. **點擊 "Records"**
4. **點擊 "Add record"**

### 步驟 3.2：添加 CNAME 記錄（www）

```
┌─────────────────────────────────────────┐
│  Add DNS record                         │
│  ┌───────────────────────────────────┐  │
│  │  Type:    [CNAME ▼]               │  │
│  │  Name:    [www                ]   │  │
│  │  Target:  [kanae-real-estate.  ]  │  │
│  │           [pages.dev           ]  │  │
│  │  Proxy:   [☁ Proxied]    ◀────   │  │ 重要！
│  │  TTL:     [Auto ▼]                │  │
│  └───────────────────────────────────┘  │
│                                         │
│  [Save]                                 │
└─────────────────────────────────────────┘
```

**填寫內容**：
- Type: `CNAME`
- Name: `www`
- Target: `kanae-real-estate.pages.dev`
- Proxy status: **Proxied**（橙色雲朵）← 重要！
- TTL: `Auto`

**點擊 "Save"**

---

### 步驟 3.3：添加 CNAME 記錄（根域名）

```
┌─────────────────────────────────────────┐
│  Add DNS record                         │
│  ┌───────────────────────────────────┐  │
│  │  Type:    [CNAME ▼]               │  │
│  │  Name:    [@                   ]  │  │
│  │  Target:  [kanae-real-estate.  ]  │  │
│  │           [pages.dev           ]  │  │
│  │  Proxy:   [☁ Proxied]    ◀────   │  │ 重要！
│  │  TTL:     [Auto ▼]                │  │
│  └───────────────────────────────────┘  │
│                                         │
│  [Save]                                 │
└─────────────────────────────────────────┘
```

**填寫內容**：
- Type: `CNAME`
- Name: `@`
- Target: `kanae-real-estate.pages.dev`
- Proxy status: **Proxied**（橙色雲朵）← 重要！
- TTL: `Auto`

**點擊 "Save"**

---

## ⚠️ 重要提醒

### Proxy Status 必須為 Proxied

```
✅ 正確：Proxied（橙色雲朵圖標 ☁）
❌ 錯誤：DNS only（灰色雲朵圖標）
```

**為什麼重要**：
- ✅ 啟用 Cloudflare CDN
- ✅ 自動配置 SSL
- ✅ DDoS 防護
- ✅ 效能優化

**如果設定為 DNS only**：
- ❌ 無 CDN 加速
- ❌ 需要手動配置 SSL
- ❌ 無 Cloudflare 保護

---

# Part 4: SSL 憑證與 HTTPS 設定

## 步驟 4.1：檢查 SSL 憑證狀態

1. **在 Cloudflare Dashboard**，點擊您的**域名**

2. **點擊左側的 "SSL/TLS"**

3. **查看 SSL/TLS 加密模式**

```
┌─────────────────────────────────────────┐
│  SSL/TLS encryption mode                │
│  ┌───────────────────────────────────┐  │
│  │  ○ Off                            │  │
│  │  ○ Flexible                       │  │
│  │  ○ Full                           │  │
│  │  ● Full (strict)      ◀────────   │  │ 選擇這個
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

4. **選擇 "Full (strict)"** 模式
   - 這是最安全的模式
   - 確保端到端加密

---

## 步驟 4.2：查看憑證狀態

1. **點擊 "Edge Certificates"**（邊緣憑證）

2. **查看 SSL 憑證狀態**

```
┌─────────────────────────────────────────┐
│  Edge Certificates                      │
│  ┌───────────────────────────────────┐  │
│  │  SSL/TLS certificate              │  │
│  │  Status: ✅ Active                │  │ ← 確認這個
│  │  Type: Universal SSL              │  │
│  │  Issued: Cloudflare Inc ECC CA-3  │  │
│  │  Valid until: [Date]              │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

**狀態說明**：
- ✅ **Active**：憑證已激活，可以使用
- ⏳ **Pending**：正在生成憑證（等待 5-15 分鐘）
- ❌ **Error**：有錯誤，需要檢查配置

**如果顯示 Pending**：
- 等待 5-15 分鐘
- Cloudflare 正在為您的域名生成 SSL 憑證
- 完成後會自動變為 Active

---

## 步驟 4.3：啟用 Always Use HTTPS

1. **在 "Edge Certificates" 頁面**

2. **找到 "Always Use HTTPS"** 選項

3. **將開關切換為 "On"**

```
┌─────────────────────────────────────────┐
│  Always Use HTTPS                       │
│  自動將所有 HTTP 請求重定向到 HTTPS      │
│                                         │
│  [OFF]  →  [ON]  ◀─────────────────    │ 開啟這個
└─────────────────────────────────────────┘
```

**作用**：
- 訪問 `http://yourdomain.com` 
- 自動轉向 `https://yourdomain.com`
- 確保所有流量都使用 HTTPS

---

## 步驟 4.4：啟用 Automatic HTTPS Rewrites

1. **在同一頁面**，找到 **"Automatic HTTPS Rewrites"**

2. **將開關切換為 "On"**

```
┌─────────────────────────────────────────┐
│  Automatic HTTPS Rewrites               │
│  自動將頁面中的 HTTP 連結改為 HTTPS      │
│                                         │
│  [OFF]  →  [ON]  ◀─────────────────    │ 開啟這個
└─────────────────────────────────────────┘
```

**作用**：
- 自動重寫頁面中的 HTTP 資源連結
- 避免混合內容警告
- 提高安全性

---

## 步驟 4.5：設定 HSTS（可選，推薦）

**HSTS** = HTTP Strict Transport Security

1. **在 "Edge Certificates" 頁面**

2. **找到 "HTTP Strict Transport Security (HSTS)"**

3. **點擊 "Enable HSTS"**

4. **配置 HSTS 設定**：

```
┌─────────────────────────────────────────┐
│  Enable HSTS                            │
│  ┌───────────────────────────────────┐  │
│  │  Max Age:                         │  │
│  │  [6 months ▼]         ◀────────   │  │ 選擇
│  │                                   │  │
│  │  ☑ Apply to subdomains            │  │ 勾選
│  │  ☑ Preload                        │  │ 勾選（可選）
│  │  ☑ No-Sniff header                │  │ 勾選
│  └───────────────────────────────────┘  │
│                                         │
│  [I Understand] [Next]                  │
└─────────────────────────────────────────┘
```

**推薦設定**：
- Max Age: **6 months**
- Apply to subdomains: **啟用**
- Preload: **啟用**（可選）
- No-Sniff header: **啟用**

5. **勾選確認框並點擊 "Enable HSTS"**

---

# Part 5: 驗證與測試

## 步驟 5.1：等待 DNS 傳播

配置完成後，需要等待 DNS 記錄傳播：

**時間預估**：
- ⏱️ 本地：幾秒到幾分鐘
- ⏱️ 全球：5-60 分鐘
- ⏱️ 最長：24-48 小時

**檢查工具**：
```
🔗 https://dnschecker.org/
輸入：www.yourdomain.com
類型：CNAME
查看：全球傳播狀態
```

---

## 步驟 5.2：訪問正式域名

1. **打開瀏覽器**（最好使用無痕模式）

2. **輸入您的域名**：
   ```
   https://www.kanae-estate.com
   或
   https://kanae-estate.com
   ```

3. **檢查網站是否正常顯示**

---

## 步驟 5.3：驗證 HTTPS 和 SSL 憑證

### 檢查 HTTPS

1. **查看瀏覽器地址欄**

```
✅ 正確顯示：
┌────────────────────────────────────┐
│ 🔒 https://www.kanae-estate.com    │ ← 有鎖頭圖標
└────────────────────────────────────┘

❌ 錯誤顯示：
┌────────────────────────────────────┐
│ ⚠️ Not secure | http://...         │ ← 沒有 HTTPS
└────────────────────────────────────┘
```

2. **確認沒有安全警告**
   - 不應該有 "Not Secure" 警告
   - 不應該有紅色警告圖標

---

### 檢查 SSL 憑證

1. **點擊地址欄的鎖頭圖標** 🔒

```
┌────────────────────────────────────┐
│  🔒 Connection is secure           │
│  ┌──────────────────────────────┐  │
│  │  Certificate (valid)         │  │ ← 點擊這裡
│  └──────────────────────────────┘  │
└────────────────────────────────────┘
```

2. **查看憑證詳情**：

```
┌────────────────────────────────────┐
│  Certificate Information           │
│  ┌──────────────────────────────┐  │
│  │  Issued to:                  │  │
│  │    www.kanae-estate.com      │  │
│  │                              │  │
│  │  Issued by:                  │  │
│  │    Cloudflare Inc ECC CA-3   │  │
│  │                              │  │
│  │  Valid from: [Date]          │  │
│  │  Valid until: [Date]         │  │
│  │                              │  │
│  │  Status: ✅ Valid            │  │
│  └──────────────────────────────┘  │
└────────────────────────────────────┘
```

**確認項目**：
- ✅ Issued to: 您的域名
- ✅ Issued by: Cloudflare
- ✅ Valid until: 未來日期
- ✅ Status: Valid

---

## 步驟 5.4：測試 HTTP 自動轉向 HTTPS

**目的**：確認 HTTP 請求會自動轉向 HTTPS

1. **在瀏覽器輸入 HTTP 版本的 URL**：
   ```
   http://www.kanae-estate.com
   ```
   注意：是 `http://`，不是 `https://`

2. **按 Enter**

3. **觀察地址欄**

```
輸入：http://www.kanae-estate.com
       ↓ 自動轉向
結果：https://www.kanae-estate.com  ← 自動變成 HTTPS
```

**驗證成功**：
- ✅ URL 自動從 `http://` 變為 `https://`
- ✅ 頁面正常顯示
- ✅ 地址欄顯示鎖頭圖標

---

## 步驟 5.5：測試不同域名版本

測試所有域名變體都能正常工作：

```bash
# 測試這些 URL（都應該可以訪問）：

1. https://www.kanae-estate.com
2. https://kanae-estate.com
3. http://www.kanae-estate.com  （應自動轉向 HTTPS）
4. http://kanae-estate.com      （應自動轉向 HTTPS）
```

**預期結果**：
- ✅ 所有版本都能訪問
- ✅ HTTP 自動轉向 HTTPS
- ✅ 內容顯示一致

---

## 步驟 5.6：使用 SSL 檢測工具深度測試

### SSL Labs Server Test

1. **前往 SSL Labs**：
   ```
   🔗 https://www.ssllabs.com/ssltest/
   ```

2. **輸入您的域名**：
   ```
   Hostname: www.kanae-estate.com
   ```

3. **點擊 "Submit"**

4. **等待測試完成**（約 1-2 分鐘）

5. **查看評分結果**：

```
┌────────────────────────────────────┐
│  SSL Server Test Results          │
│  ┌──────────────────────────────┐ │
│  │  Overall Rating: A+ ◀──────  │ │ 目標評分
│  │                              │ │
│  │  Certificate:    ✅ 100/100  │ │
│  │  Protocol:       ✅ 100/100  │ │
│  │  Key Exchange:   ✅ 90/100   │ │
│  │  Cipher:         ✅ 90/100   │ │
│  └──────────────────────────────┘ │
└────────────────────────────────────┘
```

**目標評分**：
- ✅ **A+** 或 **A**：優秀
- ⚠️ **B**：良好，但可改進
- ❌ **C** 或以下：需要修復問題

---

## 步驟 5.7：完整功能測試

在正式域名上測試所有網站功能：

```
✅ 網站功能測試清單

基本功能：
├─ [ ] 首頁正常顯示
├─ [ ] 所有圖片正常載入
├─ [ ] CSS 樣式正確應用
├─ [ ] JavaScript 正常運行
└─ [ ] 無 Console 錯誤（F12）

導航測試：
├─ [ ] 導航欄正常顯示
├─ [ ] 所有連結可以點擊
├─ [ ] 頁面間跳轉正常
└─ [ ] 語言切換功能正常

多語言測試：
├─ [ ] 日語顯示正常
├─ [ ] 中文顯示正常
├─ [ ] 英語顯示正常
└─ [ ] 語言切換無錯誤

響應式設計：
├─ [ ] 桌面版正常（>1024px）
├─ [ ] 平板版正常（768-1024px）
└─ [ ] 手機版正常（<768px）

效能測試：
├─ [ ] 首次載入 < 3 秒
├─ [ ] 後續導航快速
└─ [ ] 圖片載入流暢

安全測試：
├─ [ ] HTTPS 正常運作
├─ [ ] SSL 憑證有效
├─ [ ] HTTP 自動轉 HTTPS
└─ [ ] 無混合內容警告
```

---

# 完整驗證清單

## ✅ Cloudflare 站點設定

```
□ 域名已添加到 Cloudflare
□ Nameservers 已在註冊商處更改
□ Nameservers 已生效
□ 域名狀態顯示為 "Active"
□ DNS 記錄已正確配置
```

---

## ✅ Pages Custom Domain 設定

```
□ 已在 Pages 專案添加自定義域名
□ www.yourdomain.com 狀態為 Active
□ yourdomain.com 狀態為 Active（如有設定）
□ DNS 記錄自動創建成功
```

---

## ✅ DNS 記錄確認

```
□ www 的 CNAME 記錄存在
   - Name: www
   - Target: your-project.pages.dev
   - Proxy: Proxied ☁
□ @ 的 CNAME 記錄存在（如需要）
   - Name: @
   - Target: your-project.pages.dev
   - Proxy: Proxied ☁
□ 所有記錄的 Proxy 狀態為 Proxied
```

---

## ✅ SSL/TLS 設定確認

```
□ SSL/TLS 加密模式為 "Full (strict)"
□ SSL 憑證狀態為 "Active"
□ "Always Use HTTPS" 已啟用
□ "Automatic HTTPS Rewrites" 已啟用
□ HSTS 已啟用（可選）
```

---

## ✅ 網站訪問測試

```
□ https://www.yourdomain.com 可訪問
□ https://yourdomain.com 可訪問（如有設定）
□ 瀏覽器顯示 🔒 鎖頭圖標
□ 憑證詳情顯示正確
□ 無安全警告
```

---

## ✅ HTTP 轉 HTTPS 測試

```
□ http://www.yourdomain.com 自動轉向 HTTPS
□ http://yourdomain.com 自動轉向 HTTPS
□ 轉向後內容正常顯示
□ 無重定向循環錯誤
```

---

## ✅ SSL Labs 測試

```
□ 前往 ssllabs.com/ssltest 測試
□ 輸入域名並提交
□ 等待測試完成
□ 評分為 A 或 A+
□ 無重大安全問題
```

---

## ✅ 功能完整性測試

```
□ 所有頁面正常顯示
□ 導航功能正常
□ 語言切換正常
□ 響應式設計正常
□ 圖片和資源正常載入
□ JavaScript 功能正常
□ 無 Console 錯誤
```

---

# 常見問題排查

## 問題 1：DNS 無法解析

**症狀**：訪問域名時顯示 "DNS_PROBE_FINISHED_NXDOMAIN"

**解決方案**：

1. **檢查 DNS 記錄是否正確**
   - 進入 Cloudflare Dashboard > DNS
   - 確認 CNAME 記錄存在且正確

2. **等待 DNS 傳播**
   - 可能需要 5-60 分鐘
   - 使用 dnschecker.org 檢查傳播狀態

3. **清除本地 DNS 緩存**：
   ```bash
   # Windows
   ipconfig /flushdns
   
   # macOS
   sudo dscacheutil -flushcache
   sudo killall -HUP mDNSResponder
   
   # Linux
   sudo systemd-resolve --flush-caches
   ```

---

## 問題 2：SSL 憑證顯示 "Pending"

**症狀**：憑證狀態一直顯示 "Pending"

**解決方案**：

1. **確認 DNS 記錄的 Proxy 狀態**
   - 必須為 "Proxied"（橙色雲朵）
   - 如果是 "DNS only"，改為 "Proxied"

2. **等待憑證生成**
   - 通常需要 5-15 分鐘
   - 最長可能需要 24 小時

3. **檢查域名驗證**
   - Cloudflare 需要驗證域名所有權
   - 確保域名 DNS 已指向 Cloudflare

4. **如果超過 24 小時仍未解決**：
   - 刪除自定義域名
   - 重新添加一次
   - 或聯繫 Cloudflare 支援

---

## 問題 3：出現 "Too Many Redirects" 錯誤

**症狀**：瀏覽器顯示 "ERR_TOO_MANY_REDIRECTS"

**解決方案**：

1. **檢查 SSL/TLS 加密模式**
   - 進入 Cloudflare Dashboard > SSL/TLS
   - 確保設定為 "Full (strict)"
   - 不要使用 "Flexible"

2. **檢查 Pages 專案沒有重定向規則衝突**

3. **清除瀏覽器 Cookie 和緩存**：
   - 按 Ctrl+Shift+Delete（Windows）
   - 或 Cmd+Shift+Delete（macOS）
   - 清除瀏覽數據

4. **使用無痕模式測試**

---

## 問題 4：根域名無法訪問

**症狀**：`www.yourdomain.com` 可以訪問，但 `yourdomain.com` 不行

**解決方案**：

1. **確認已為根域名添加 DNS 記錄**
   - 進入 Cloudflare Dashboard > DNS
   - 檢查是否有 Name 為 "@" 的 CNAME 記錄

2. **在 Pages 專案中同時添加兩個域名**：
   - `www.yourdomain.com`
   - `yourdomain.com`

3. **檢查是否有 Page Rules 影響根域名**

---

## 問題 5：SSL Labs 評分低

**症狀**：SSL Labs 測試評分為 B 或 C

**解決方案**：

1. **啟用 HSTS**
   - Cloudflare Dashboard > SSL/TLS > Edge Certificates
   - Enable HSTS

2. **檢查 TLS 版本**
   - 確保支持 TLS 1.2 和 TLS 1.3
   - 禁用舊版本協議

3. **更新加密套件**
   - Cloudflare 會自動管理
   - 通常不需要手動配置

---

## 問題 6：Pages 找不到 Custom Domains 選項

**症狀**：Settings 中沒有 Custom domains 選項

**解決方案**：

1. **確認使用的是 Pages（不是 Workers）**
   - Workers 和 Pages 是不同的服務
   - 只有 Pages 有 Custom domains

2. **確認專案已成功部署**
   - 至少要有一次成功的部署

3. **檢查帳號權限**
   - 確保有管理員權限

---

# 時間預估

| 步驟 | 預估時間 |
|------|---------|
| Part 1: 添加域名到 Cloudflare | 5-10 分鐘 |
| 等待 nameservers 生效 | 5-60 分鐘 |
| Part 2: 設定 Custom Domain | 3-5 分鐘 |
| Part 3: DNS 記錄配置 | 2-3 分鐘 |
| 等待 DNS 傳播 | 5-60 分鐘 |
| Part 4: SSL/TLS 設定 | 5-10 分鐘 |
| 等待 SSL 憑證生成 | 5-15 分鐘 |
| Part 5: 驗證與測試 | 10-15 分鐘 |
| **總計（不含等待）** | **30-58 分鐘** |
| **總計（含等待）** | **40-158 分鐘** |

---

# 快速參考

## 關鍵設定摘要

```yaml
# Cloudflare 域名設定
Nameservers: 使用 Cloudflare 提供的
DNS 傳播時間: 5-60 分鐘

# Pages Custom Domain
添加位置: Workers & Pages > Project > Custom domains
域名格式: www.yourdomain.com 和 yourdomain.com

# DNS 記錄
類型: CNAME
Name: www 和 @
Target: your-project.pages.dev
Proxy: Proxied ☁（重要！）

# SSL/TLS 設定
加密模式: Full (strict)
Always Use HTTPS: ON
Automatic HTTPS Rewrites: ON
HSTS: 啟用（推薦）
```

---

## 驗證命令

```bash
# 檢查 DNS 記錄
nslookup www.yourdomain.com
dig www.yourdomain.com

# 檢查 SSL 憑證
openssl s_client -connect www.yourdomain.com:443 -servername www.yourdomain.com

# 測試 HTTP 轉向
curl -I http://www.yourdomain.com
```

---

# 總結

完成以上所有步驟後，您的網站應該：

1. ✅ 可以通過正式域名訪問
2. ✅ SSL 憑證已正確配置並處於 Active 狀態
3. ✅ HTTP 請求自動重定向到 HTTPS
4. ✅ 享有 Cloudflare 的 CDN 加速
5. ✅ 獲得 DDoS 防護和安全保護
6. ✅ SSL Labs 評分為 A 或 A+

---

**文檔版本**: 2.0.0  
**最後更新**: 2026-01-12  
**適用於**: Cloudflare Pages + 自定義域名

---

**🎉 恭喜！完成域名綁定後，您的網站就可以使用正式域名訪問了！**
