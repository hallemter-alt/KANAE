# Cloudflare Pages デプロイガイド

## 📋 目次

1. [初回デプロイ設定](#初回デプロイ設定)
2. [Cloudflare Pages 設定値](#cloudflare-pages-設定値)
3. [日常の更新フロー](#日常の更新フロー)
4. [デプロイ確認手順](#デプロイ確認手順)
5. [トラブルシューティング](#トラブルシューティング)

---

## 初回デプロイ設定

### 前提条件

- ✅ GitHub アカウント
- ✅ Cloudflare アカウント（無料プランで OK）
- ✅ このプロジェクトが GitHub にプッシュ済み

### Step 1: GitHub にプッシュ

```bash
cd /home/user/webapp

# すべての変更をコミット
git add .
git commit -m "chore: Add Cloudflare Pages configuration"

# GitHub にプッシュ
git push origin main
```

### Step 2: Cloudflare Dashboard でプロジェクト作成

1. **Cloudflare Dashboard にログイン**
   - https://dash.cloudflare.com

2. **Pages セクションに移動**
   - 左サイドバーから「Workers & Pages」をクリック
   - 「Create application」ボタンをクリック
   - 「Pages」タブを選択
   - 「Connect to Git」をクリック

3. **GitHub 連携**
   - 「Connect GitHub」をクリック
   - Cloudflare に GitHub アクセスを許可
   - リポジトリを選択：`webapp` (または実際のリポジトリ名)

4. **プロジェクト設定**
   - 次のセクションの設定値を入力

---

## Cloudflare Pages 設定値

### 🔧 ビルド設定（Build Configuration）

| 項目 | 設定値 | 説明 |
|------|--------|------|
| **Project name** | `kanae-real-estate` | プロジェクト名（URL になる） |
| **Production branch** | `main` | 本番環境のブランチ |
| **Framework preset** | `Next.js` | フレームワーク選択 |
| **Build command** | `npm run build` | ビルドコマンド |
| **Build output directory** | `.next` | ビルド出力ディレクトリ |

### 📝 詳細設定

```yaml
# ビルド設定
Framework preset: Next.js
Build command: npm run build
Build output directory: .next

# 環境変数（Environment variables）
NODE_VERSION: 20
NEXT_TELEMETRY_DISABLED: 1

# Root directory（プロジェクトのルート）
Root directory: / （デフォルトのまま）

# Production branch
Production branch: main
```

### 🎨 実際の設定画面での入力例

```
┌─────────────────────────────────────────────┐
│ Set up builds and deployments               │
├─────────────────────────────────────────────┤
│                                             │
│ Project name                                │
│ ┌─────────────────────────────────────────┐ │
│ │ kanae-real-estate                       │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ Production branch                           │
│ ┌─────────────────────────────────────────┐ │
│ │ main                                    │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ Framework preset                            │
│ ┌─────────────────────────────────────────┐ │
│ │ Next.js                            ▼   │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ Build command                               │
│ ┌─────────────────────────────────────────┐ │
│ │ npm run build                           │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ Build output directory                      │
│ ┌─────────────────────────────────────────┐ │
│ │ .next                                   │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ Root directory (advanced)                   │
│ ┌─────────────────────────────────────────┐ │
│ │ /                                       │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ Environment variables (advanced)            │
│ ┌─────────────────────────────────────────┐ │
│ │ + Add variable                          │ │
│ │   NODE_VERSION = 20                     │ │
│ │   NEXT_TELEMETRY_DISABLED = 1           │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│                      [Save and Deploy]      │
└─────────────────────────────────────────────┘
```

### ⚠️ 重要な設定ポイント

1. **Framework preset は必ず "Next.js" を選択**
   - "Static HTML" や "Create React App" を選ばないこと
   - Next.js の SSR 機能を使うため

2. **Build output directory は `.next`**
   - `out` ではない（Static Export の場合のみ `out`）
   - SSR 対応のため `.next` を指定

3. **Node.js バージョンは 20 を推奨**
   - Next.js 15 は Node.js 18 以上が必要
   - 環境変数 `NODE_VERSION=20` で指定

4. **環境変数の設定**
   - `NEXT_TELEMETRY_DISABLED=1` で匿名統計を無効化
   - 必要に応じて API キーなどを追加

---

## 日常の更新フロー

### 🔄 通常の更新手順（Automatic Deployment）

Cloudflare Pages は GitHub と連携しているため、`main` ブランチにプッシュすると**自動的に**ビルド＆デプロイされます。

#### Step 1: ローカルで開発

```bash
cd /home/user/webapp

# 開発サーバーで確認
npm run dev

# ブラウザで http://localhost:3000 を開いて確認
```

#### Step 2: 変更をコミット

```bash
# ファイルの変更を確認
git status

# 変更をステージング
git add .

# コミット（規範的なコミットメッセージを使用）
git commit -m "feat: Add new feature"
# または
git commit -m "fix: Fix navigation issue on mobile"
# または
git commit -m "docs: Update README"
```

**コミットメッセージの規範**:
- `feat:` - 新機能
- `fix:` - バグ修復
- `docs:` - ドキュメント更新
- `style:` - コードフォーマット
- `refactor:` - リファクタリング
- `perf:` - パフォーマンス改善
- `test:` - テスト追加
- `chore:` - ビルド/設定変更

#### Step 3: GitHub にプッシュ

```bash
# main ブランチにプッシュ
git push origin main
```

#### Step 4: 自動デプロイ開始 🚀

プッシュ後、自動的に以下が実行されます：

1. **GitHub → Cloudflare 通知**
   - GitHub が Cloudflare に変更を通知
   - 数秒以内に反応

2. **Cloudflare Pages ビルド開始**
   - 依存関係のインストール（`npm install`）
   - ビルド実行（`npm run build`）
   - 約 2-5 分かかる

3. **デプロイ完了**
   - ビルド成功後、自動的に公開
   - グローバル CDN に配信

### 📊 デプロイ状況の確認

```bash
# プッシュ直後
git push origin main

# ターミナルに表示される
# Enumerating objects: 5, done.
# Counting objects: 100% (5/5), done.
# ...
# To https://github.com/username/webapp.git
#    abc1234..def5678  main -> main
```

その後：
1. Cloudflare Dashboard を開く
2. プロジェクトを選択
3. 「Deployments」タブでビルド状況を確認

### 🌿 ブランチ戦略（Optional）

**開発用ブランチを使う場合**:

```bash
# 新しいブランチを作成
git checkout -b feature/new-search

# 開発とコミット
# ...

# GitHub にプッシュ
git push origin feature/new-search

# GitHub で Pull Request を作成
# レビュー後 main にマージ → 自動デプロイ
```

Cloudflare Pages は以下をサポート：
- **Production deployments**: `main` ブランチ
- **Preview deployments**: その他のブランチ（自動的にプレビュー URL を生成）

---

## デプロイ確認手順

### ✅ Step 1: Cloudflare Dashboard で確認

1. **Dashboard にアクセス**
   ```
   https://dash.cloudflare.com
   ```

2. **プロジェクトを選択**
   - 「Workers & Pages」→「kanae-real-estate」

3. **Deployments タブを確認**
   - 最新のデプロイが表示される
   - ステータスアイコンの意味：
     - 🟡 Building... - ビルド中
     - ✅ Success - 成功
     - ❌ Failed - 失敗

### 📊 デプロイステータスの詳細

#### ビルド中（Building）

```
┌──────────────────────────────────────────┐
│ ● Building                                │
│                                          │
│ ┌────────────────────────────────────┐  │
│ │ ████████░░░░░░░░░░░░░░░░░░  45%    │  │
│ └────────────────────────────────────┘  │
│                                          │
│ Installing dependencies...              │
│   npm install                           │
│                                          │
│ Estimated time: 2-5 minutes             │
└──────────────────────────────────────────┘
```

#### 成功（Success）

```
┌──────────────────────────────────────────┐
│ ✅ Deployment successful                  │
│                                          │
│ Deployed to:                             │
│ https://kanae-real-estate.pages.dev     │
│                                          │
│ Build time: 3m 42s                       │
│ Deploy time: 15s                         │
│                                          │
│ [View deployment] [View logs]            │
└──────────────────────────────────────────┘
```

#### 失敗（Failed）

```
┌──────────────────────────────────────────┐
│ ❌ Deployment failed                      │
│                                          │
│ Error: Build failed                      │
│ Exit code: 1                             │
│                                          │
│ [View full logs] [Retry deployment]      │
└──────────────────────────────────────────┘
```

### 🌐 Step 2: ウェブサイトにアクセス

デプロイ成功後、以下の URL でアクセス可能：

```
https://kanae-real-estate.pages.dev
```

または

```
https://<branch-name>.<project-name>.pages.dev
```

**確認項目**:
- ✅ ページが正常に表示される
- ✅ ナビゲーションが動作する
- ✅ 画像が表示される
- ✅ 言語切替が機能する
- ✅ レスポンシブデザインが正しい

### 🔍 Step 3: ビルドログの確認

#### ログの見方

1. **Dashboard でデプロイをクリック**
   - 「Deployments」タブ
   - 該当するデプロイをクリック

2. **「View build logs」をクリック**

3. **ログの内容を確認**

#### 正常なログの例

```bash
# 依存関係のインストール
12:34:56 | Installing dependencies...
12:34:58 | npm install
12:35:30 | added 350 packages in 32s

# ビルド実行
12:35:31 | Building application...
12:35:32 | npm run build
12:35:35 | > kanae-real-estate@0.1.0 build
12:35:35 | > next build
12:35:38 |    ▲ Next.js 15.1.6
12:35:40 |    - Environments: .env.production
12:35:45 |    ✓ Creating an optimized production build
12:36:15 |    ✓ Compiled successfully
12:36:20 |    ✓ Linting and checking validity of types
12:36:25 |    ✓ Collecting page data
12:36:30 |    ✓ Generating static pages (8/8)
12:36:32 |    ✓ Collecting build traces
12:36:35 |    ✓ Finalizing page optimization
12:36:36 |
12:36:36 | Route (app)                              Size     First Load JS
12:36:36 | ┌ ○ /                                    5.2 kB         95 kB
12:36:36 | └ ○ /_not-found                         0 B            0 B
12:36:36 |
12:36:36 | Build completed successfully!

# デプロイ
12:36:37 | Deploying to Cloudflare Pages...
12:36:45 | ✓ Deployment successful
12:36:45 | URL: https://kanae-real-estate.pages.dev
```

#### エラーログの例

```bash
12:34:56 | Installing dependencies...
12:35:30 | npm install completed

12:35:31 | Building application...
12:35:32 | npm run build
12:35:35 | > next build
12:35:38 |    ▲ Next.js 15.1.6
12:35:40 |    × Failed to compile
12:35:41 | 
12:35:41 | ./components/Navbar.tsx:10:5
12:35:41 | Type error: Property 'title' does not exist on type...
12:35:42 | 
12:35:42 |   8 |   return (
12:35:42 |   9 |     <nav>
12:35:42 | > 10 |       {props.title}
12:35:42 |      |       ^
12:35:42 |  11 |     </nav>
12:35:42 |
12:35:43 | Error: Build failed with exit code 1
```

### 🔔 Step 4: 通知設定（Optional）

**Discord/Slack 通知を設定**:

1. Dashboard → プロジェクト → Settings
2. Notifications セクション
3. Webhook URL を設定

通知内容：
- ✅ デプロイ成功
- ❌ デプロイ失敗
- 🔄 デプロイ開始

---

## トラブルシューティング

### ❌ ビルドが失敗する

#### 問題 1: `npm install` でエラー

**エラーメッセージ**:
```
npm ERR! code ERESOLVE
npm ERR! ERESOLVE could not resolve
```

**解決方法**:
```bash
# ローカルで確認
cd /home/user/webapp
rm -rf node_modules package-lock.json
npm install

# 問題なければコミット＆プッシュ
git add package-lock.json
git commit -m "fix: Update package-lock.json"
git push origin main
```

#### 問題 2: TypeScript エラー

**エラーメッセージ**:
```
Type error: Property 'xxx' does not exist
```

**解決方法**:
```bash
# ローカルで型チェック
npm run lint

# エラーを修正してからプッシュ
```

#### 問題 3: ビルドタイムアウト

**エラーメッセージ**:
```
Error: Build exceeded maximum time limit
```

**解決方法**:
- 依存関係を減らす
- 不要なパッケージを削除
- ビルドキャッシュの問題の場合：
  - Dashboard → Settings → "Clear build cache"

### ❌ デプロイは成功するが、サイトが表示されない

#### 問題 1: 404 Not Found

**原因**: ルーティング設定の問題

**解決方法**:

1. `_routes.json` ファイルを作成（自動生成されるはずだが、念のため）

2. `next.config.ts` を確認：
```typescript
const nextConfig: NextConfig = {
  output: 'standalone', // これが重要
};
```

#### 問題 2: 画像が表示されない

**原因**: Next.js Image Optimization が Cloudflare で動作しない

**解決方法**:

`next.config.ts` に追加：
```typescript
images: {
  unoptimized: true, // Cloudflare Pages では必須
}
```

#### 問題 3: API Routes が動作しない

**原因**: Cloudflare Pages は Node.js ランタイムをサポートしていない

**解決方法**:
- API Routes を Cloudflare Workers に移行
- または Cloudflare Functions を使用

### ❌ デプロイは成功するが、更新が反映されない

#### 問題: キャッシュの問題

**解決方法**:

1. **ブラウザキャッシュをクリア**
   - Chrome: `Ctrl + Shift + R` (Windows/Linux)
   - Chrome: `Cmd + Shift + R` (Mac)

2. **Cloudflare CDN キャッシュをパージ**
   - Dashboard → プロジェクト → Settings
   - "Purge build cache" をクリック

3. **再デプロイ**
   - Dashboard → Deployments
   - 最新のデプロイの "⋮" メニュー → "Retry deployment"

### 🔍 ログの詳細を確認する方法

1. **Dashboard のログ**
   ```
   Deployments → [該当するデプロイ] → View build logs
   ```

2. **リアルタイムログ（Wrangler CLI 使用）**
   ```bash
   npm install -g wrangler
   wrangler pages deployment tail
   ```

3. **Functions ログ（API Routes を使う場合）**
   ```bash
   wrangler pages deployment tail --format json
   ```

### 📞 サポートリソース

- **Cloudflare Community**: https://community.cloudflare.com/
- **Discord**: Cloudflare Developers Discord
- **ドキュメント**: https://developers.cloudflare.com/pages/
- **Status Page**: https://www.cloudflarestatus.com/

---

## 補足：カスタムドメインの設定

### 独自ドメインを使う場合

1. **Dashboard → プロジェクト → Custom domains**

2. **ドメインを追加**
   ```
   www.kanae-tokyo.com
   kanae-tokyo.com
   ```

3. **DNS レコードを設定**
   - Cloudflare が自動的に設定してくれる
   - または手動で CNAME レコードを追加

4. **SSL 証明書**
   - 自動的に発行される（Let's Encrypt）
   - 数分で有効化

---

## まとめ

### ✅ セットアップチェックリスト

- [ ] GitHub にリポジトリをプッシュ済み
- [ ] Cloudflare アカウント作成済み
- [ ] Cloudflare Pages でプロジェクト作成
- [ ] ビルド設定を正しく入力
- [ ] 初回デプロイが成功
- [ ] `https://<project-name>.pages.dev` でアクセス可能

### 🔄 日常の更新フロー（再掲）

```bash
# 1. ローカルで開発
npm run dev

# 2. 変更をコミット
git add .
git commit -m "feat: Add new feature"

# 3. GitHub にプッシュ
git push origin main

# 4. 自動的にビルド＆デプロイされる（待つだけ）

# 5. https://<project-name>.pages.dev で確認
```

### 🎯 重要なポイント

1. **Framework preset は "Next.js" を選択**
2. **Build output directory は `.next`**
3. **main ブランチにプッシュすると自動デプロイ**
4. **ビルドログで問題を確認**
5. **エラー時は該当セクションを参照**

---

**作成日**: 2026-01-12  
**対象プロジェクト**: KANAE Real Estate Website  
**フレームワーク**: Next.js 15  
**デプロイ先**: Cloudflare Pages
