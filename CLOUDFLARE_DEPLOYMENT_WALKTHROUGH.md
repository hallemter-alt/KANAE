# 🚀 Cloudflare Pages 部署實戰指南

## 📋 部署前檢查清單

在開始部署前，請確認以下項目：

- ✅ 代碼已推送到 GitHub
- ✅ Next.js 配置已優化（output: 'standalone'）
- ✅ 擁有 Cloudflare 帳號
- ✅ GitHub 帳號可訪問

---

## 🎯 部署方式選擇

Cloudflare Pages 提供兩種部署方式：

### 方式 1：通過 Cloudflare Dashboard（推薦）⭐
- ✅ **優點**：圖形介面，易於操作，自動 CI/CD
- ✅ **適合**：首次部署，長期維護
- ⏱️ **時間**：15-30 分鐘

### 方式 2：使用 Wrangler CLI
- ✅ **優點**：命令行快速部署
- ✅ **適合**：開發環境測試，快速迭代
- ⏱️ **時間**：5-10 分鐘

**建議**：首次部署使用**方式 1**，之後可使用方式 2 進行快速更新。

---

## 🌐 方式 1：Cloudflare Dashboard 部署（詳細步驟）

### 步驟 1：登入 Cloudflare Dashboard

1. 打開瀏覽器，前往：
   ```
   https://dash.cloudflare.com/
   ```

2. 使用您的 Cloudflare 帳號登入

3. 如果還沒有帳號：
   - 點擊 "Sign Up"
   - 使用郵箱註冊
   - 驗證郵箱

---

### 步驟 2：創建 Pages 專案

1. 在 Cloudflare Dashboard 左側導航欄，點擊 **"Workers & Pages"**

   ```
   ┌─────────────────────────────────┐
   │  Cloudflare Dashboard           │
   │  ├─ 🏠 Home                     │
   │  ├─ 📊 Analytics                │
   │  ├─ 🌐 Websites                 │
   │  ├─ ⚡ Workers & Pages  ◀────   │ 點擊這裡
   │  ├─ 🔐 Zero Trust               │
   │  └─ ...                         │
   └─────────────────────────────────┘
   ```

2. 點擊右上角的 **"Create application"**（創建應用程式）按鈕

3. 選擇 **"Pages"** 標籤

4. 點擊 **"Connect to Git"**（連接到 Git）

   ```
   ┌──────────────────────────────────────┐
   │  Create an application               │
   │  ┌────────────┬────────────────────┐ │
   │  │  Workers   │  Pages             │ │ ← 選擇這個標籤
   │  └────────────┴────────────────────┘ │
   │                                      │
   │  [Connect to Git]  ◀────────────────── 點擊這個按鈕
   │                                      │
   └──────────────────────────────────────┘
   ```

---

### 步驟 3：連接 GitHub 倉庫

1. 選擇 **GitHub** 作為 Git 提供商

2. 如果這是第一次連接：
   - 點擊 **"Connect GitHub"**
   - 在彈出的 GitHub 授權視窗中：
     - 登入您的 GitHub 帳號
     - 選擇授權範圍：
       - **"Only select repositories"**（僅選擇的倉庫）- 推薦
       - 或 **"All repositories"**（所有倉庫）
     - 選擇 **KANAE** 倉庫
     - 點擊 **"Install & Authorize"**

3. 授權完成後，您會看到倉庫列表

4. 找到並點擊 **"KANAE"** 倉庫

   ```
   ┌──────────────────────────────────────┐
   │  Select a repository                 │
   │  ┌────────────────────────────────┐  │
   │  │ 🔍 Search repositories         │  │
   │  └────────────────────────────────┘  │
   │                                      │
   │  ☐ hallemter-alt/other-repo         │
   │  ☑ hallemter-alt/KANAE      ◀────── 選擇這個
   │  ☐ hallemter-alt/another-repo       │
   │                                      │
   └──────────────────────────────────────┘
   ```

5. 點擊 **"Begin setup"**（開始設定）

---

### 步驟 4：配置構建設定 ⚙️

現在是**最關鍵**的步驟！請仔細配置以下選項：

#### 4.1 專案名稱（Project name）

```
專案名稱：kanae-real-estate
```

- 這將成為您的預設域名：`kanae-real-estate.pages.dev`
- 只能使用小寫字母、數字和連字符
- 建議使用有意義的名稱

#### 4.2 生產分支（Production branch）

```
Production branch：main
```

- 選擇 `main` 分支
- 每次推送到此分支都會觸發自動部署

#### 4.3 Framework preset（框架預設）

```
Framework preset：Next.js (SSR)
```

⚠️ **重要**：
- 不要選擇 "Next.js (Static Export)"
- 必須選擇 **"Next.js"** 或 **"Next.js (SSR)"**
- 我們的專案使用 Next.js 15 的 App Router，需要 SSR 支援

#### 4.4 Build 設定

**Build command**（構建命令）：
```bash
npm run build
```

**Build output directory**（輸出目錄）：
```
.next
```

⚠️ **注意**：
- 對於 Next.js 15，輸出目錄是 `.next`
- 不是 `out` 或 `dist`

#### 4.5 Root directory（根目錄）

```
Root directory：/ （留空或選擇根目錄）
```

#### 4.6 環境變數（Environment variables）- 可選

目前我們不需要設定環境變數，稍後可以在設定中添加。

**完整配置視覺化**：

```
┌─────────────────────────────────────────────────┐
│  Set up builds and deployments                  │
├─────────────────────────────────────────────────┤
│  Project name:                                  │
│  [kanae-real-estate                        ]    │
│                                                 │
│  Production branch:                             │
│  [main                                     ▼]   │
│                                                 │
│  Framework preset:                              │
│  [Next.js (SSR)                            ▼]   │
│                                                 │
│  Build command:                                 │
│  [npm run build                            ]    │
│                                                 │
│  Build output directory:                        │
│  [.next                                    ]    │
│                                                 │
│  Root directory (optional):                     │
│  [/                                        ]    │
│                                                 │
│  Environment variables (optional):              │
│  [+ Add variable]                               │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

### 步驟 5：開始部署

1. 確認所有設定無誤後，點擊 **"Save and Deploy"**（保存並部署）

2. Cloudflare 會開始：
   - ✅ Clone 您的 GitHub 倉庫
   - ✅ 安裝依賴（npm install）
   - ✅ 構建專案（npm run build）
   - ✅ 部署到 Cloudflare Pages

3. 部署過程視覺化：

   ```
   ┌──────────────────────────────────────┐
   │  Deploying kanae-real-estate         │
   │                                      │
   │  ✅ Initializing build environment   │
   │  ⏳ Cloning repository                │
   │  ⏳ Installing dependencies           │
   │  ⏳ Building project                  │
   │  ⏳ Deploying                         │
   │                                      │
   │  [View build logs]                   │
   └──────────────────────────────────────┘
   ```

---

### 步驟 6：監控構建過程

1. 在部署頁面，點擊 **"View build log"**（查看構建日誌）

2. 您會看到實時的構建輸出：

   ```
   12:00:00 | Cloning repository...
   12:00:05 | Installing dependencies...
   12:00:35 | > kanae-real-estate@0.1.0 build
   12:00:35 | > next build
   12:00:40 | Creating an optimized production build...
   12:01:20 | Build completed successfully!
   12:01:25 | Deploying to Cloudflare Pages...
   12:01:30 | Success! Deployed to kanae-real-estate.pages.dev
   ```

3. **常見構建時間**：
   - 首次構建：2-5 分鐘
   - 後續構建：1-3 分鐘

---

### 步驟 7：驗證部署成功

1. 構建完成後，您會看到：
   ```
   ✅ Success! Your site is live at:
   https://kanae-real-estate.pages.dev
   ```

2. 點擊這個 URL 或在新標籤頁中打開

3. **驗證清單**：
   - [ ] 網站可以正常訪問
   - [ ] 首頁內容正確顯示
   - [ ] 導航欄功能正常
   - [ ] 語言切換可以工作
   - [ ] 響應式設計正常（測試手機/平板視圖）
   - [ ] 沒有 404 錯誤
   - [ ] 圖片正常載入

4. 測試響應式設計：
   - 按 F12 打開開發者工具
   - 點擊設備工具欄圖標（或按 Ctrl+Shift+M）
   - 測試不同設備尺寸：
     - 📱 iPhone SE (375x667)
     - 📱 iPhone 12 Pro (390x844)
     - 📱 iPad (768x1024)
     - 💻 Desktop (1920x1080)

---

### 步驟 8：配置自動部署（已自動設定）

✅ **好消息**：通過 GitHub 連接，自動部署已經設定好了！

**工作原理**：
```
您推送代碼到 GitHub
     ↓
GitHub webhook 通知 Cloudflare
     ↓
Cloudflare 自動開始構建
     ↓
構建完成後自動部署
     ↓
網站更新完成
```

**測試自動部署**：
```bash
# 在本地進行一個小修改
cd /home/user/webapp
echo "測試自動部署" >> test.txt

# 提交並推送
git add test.txt
git commit -m "test: Verify automatic deployment"
git push origin main

# 然後到 Cloudflare Dashboard 查看自動觸發的構建
```

---

## ⚡ 方式 2：使用 Wrangler CLI 部署

如果您想使用命令行部署，可以使用 Wrangler：

### 安裝 Wrangler

```bash
cd /home/user/webapp
npm install -g wrangler

# 或使用 npx（不需要全局安裝）
npx wrangler --version
```

### 登入 Cloudflare

```bash
npx wrangler login
```

這會打開瀏覽器讓您授權。

### 部署專案

```bash
cd /home/user/webapp

# 首次部署
npx wrangler pages deploy .next --project-name=kanae-real-estate

# 之後的部署
npm run build
npx wrangler pages deploy .next --project-name=kanae-real-estate
```

**注意**：使用 Wrangler 部署不會設定自動 CI/CD，需要手動執行部署命令。

---

## 🔍 部署後檢查

### 1. 檢查部署狀態

在 Cloudflare Dashboard：
1. 進入 **Workers & Pages**
2. 點擊您的專案 **kanae-real-estate**
3. 查看 **Deployments** 標籤

您會看到：
```
┌────────────────────────────────────────────┐
│  Deployments                               │
├────────────────────────────────────────────┤
│  Production                                │
│  ✅ main (fc9c9a8)  2 minutes ago         │
│  Status: Active                            │
│  Domain: kanae-real-estate.pages.dev       │
│  [View deployment] [View build log]        │
└────────────────────────────────────────────┘
```

---

### 2. 檢查構建日誌

如果遇到問題，查看構建日誌：

1. 點擊部署記錄
2. 點擊 **"View build log"**
3. 查找錯誤訊息

**常見錯誤**：
- ❌ `Module not found`: 依賴未安裝
- ❌ `Build failed`: 構建腳本錯誤
- ❌ `Out of memory`: 構建資源不足

---

### 3. 測試網站功能

使用以下測試清單：

```
✅ 功能測試清單
├─ [ ] 首頁載入
├─ [ ] 導航欄
│   ├─ [ ] 滾動時背景變化
│   ├─ [ ] 語言切換（日/中/英）
│   └─ [ ] 移動端選單
├─ [ ] Hero 區域
│   ├─ [ ] 背景圖片載入
│   ├─ [ ] 動畫效果
│   └─ [ ] CTA 按鈕
├─ [ ] Services 區域
│   └─ [ ] 4 個服務卡片顯示
├─ [ ] Philosophy 區域
│   └─ [ ] 企業理念內容
├─ [ ] Features 區域
│   └─ [ ] 6 個特色展示
├─ [ ] Stats 區域
│   └─ [ ] 數據統計
├─ [ ] CTA 區域
│   └─ [ ] 行動呼籲
└─ [ ] Footer
    ├─ [ ] 公司資訊
    └─ [ ] 連結正常
```

---

## 🎯 部署完成後的配置

### 1. 自定義域名（可選）

如果您有自己的域名，可以綁定：

1. 在專案頁面，點擊 **"Custom domains"**
2. 點擊 **"Set up a custom domain"**
3. 輸入您的域名（例如：www.kanae-estate.com）
4. 按照提示配置 DNS

📗 **詳細指南**：參考 `CLOUDFLARE_CUSTOM_DOMAIN_GUIDE.md`

---

### 2. 環境變數設定（如需要）

1. 進入專案的 **"Settings"** 標籤
2. 選擇 **"Environment variables"**
3. 點擊 **"Add variable"**
4. 添加所需的環境變數：
   ```
   Name: NEXT_PUBLIC_API_URL
   Value: https://api.example.com
   ```

5. 選擇應用範圍：
   - Production（生產環境）
   - Preview（預覽環境）

---

### 3. Build 設定調整（如需要）

如果需要修改構建設定：

1. 進入 **"Settings"** > **"Builds & deployments"**
2. 修改：
   - Build command
   - Build output directory
   - Root directory
3. 點擊 **"Save"**

下次部署會使用新的設定。

---

## 📊 效能優化建議

### 1. 啟用 Cloudflare CDN

✅ 已自動啟用！Cloudflare Pages 自動使用全球 CDN。

### 2. 檢查載入速度

使用工具測試：
- 🔗 [PageSpeed Insights](https://pagespeed.web.dev/)
- 🔗 [GTmetrix](https://gtmetrix.com/)
- 🔗 [WebPageTest](https://www.webpagetest.org/)

目標：
- ✅ Performance Score > 90
- ✅ First Contentful Paint < 1.5s
- ✅ Largest Contentful Paint < 2.5s

---

### 3. 圖片優化

我們已在 `next.config.ts` 中設定：
```typescript
images: {
  domains: ['images.unsplash.com'],
  unoptimized: true, // Cloudflare Pages 需要
}
```

考慮：
- 使用 Cloudflare Images（付費功能）
- 或使用 CDN 託管圖片

---

## 🐛 常見問題排查

### 問題 1：構建失敗 - "Module not found"

**原因**：依賴未正確安裝

**解決**：
```bash
# 本地測試
cd /home/user/webapp
rm -rf node_modules package-lock.json
npm install
npm run build

# 如果本地成功，推送後重新部署
git add .
git commit -m "fix: Update dependencies"
git push origin main
```

---

### 問題 2：部署成功但頁面空白

**原因**：Next.js 配置問題或路由錯誤

**檢查**：
1. 查看瀏覽器 Console（F12）
2. 檢查 Network 標籤是否有 404 錯誤
3. 確認 `next.config.ts` 配置正確

**解決**：
```typescript
// next.config.ts
const nextConfig: NextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true, // 確保這個設定存在
  },
  output: 'standalone', // 確保使用 standalone 模式
};
```

---

### 問題 3：圖片無法載入

**原因**：Cloudflare Pages 不支援 Next.js Image Optimization

**解決**：確保 `next.config.ts` 中有：
```typescript
images: {
  unoptimized: true,
}
```

---

### 問題 4：部署超時

**原因**：構建時間過長

**解決**：
1. 檢查是否有大型依賴
2. 優化構建過程
3. 聯繫 Cloudflare 支援增加超時時間

---

## 📱 移動端測試

部署完成後，請在真實設備上測試：

### iOS 測試
1. 在 iPhone 上打開 Safari
2. 訪問 `https://kanae-real-estate.pages.dev`
3. 測試：
   - [ ] 觸控操作
   - [ ] 滾動流暢度
   - [ ] 語言切換
   - [ ] 選單展開

### Android 測試
1. 在 Android 手機上打開 Chrome
2. 訪問網站
3. 測試相同功能

---

## 🎉 部署完成檢查清單

完成以下所有項目，即可確認部署成功：

```
✅ 部署完成檢查清單
├─ [ ] Cloudflare Pages 專案已創建
├─ [ ] GitHub 倉庫已連接
├─ [ ] 首次構建成功完成
├─ [ ] 網站可通過 .pages.dev 訪問
├─ [ ] 自動部署已設定（GitHub webhook）
├─ [ ] 所有頁面功能正常
├─ [ ] 響應式設計在各設備正常
├─ [ ] 語言切換功能正常
├─ [ ] 圖片正常載入
├─ [ ] 無 Console 錯誤
├─ [ ] 效能測試分數良好
└─ [ ] 移動端測試通過
```

---

## 🚀 後續步驟

部署完成後，您可以：

1. **設定自定義域名**
   - 📗 參考：`CLOUDFLARE_CUSTOM_DOMAIN_GUIDE.md`
   - ⏱️ 時間：35-100 分鐘

2. **監控網站狀態**
   - 在 Cloudflare Dashboard 查看 Analytics
   - 設定 Uptime 監控

3. **繼續開發新功能**
   - 物件搜尋功能
   - 物件詳情頁面
   - 用戶系統

4. **SEO 優化**
   - 添加 sitemap.xml
   - 配置 robots.txt
   - 優化 meta tags

---

## 📞 需要幫助？

### 查看文檔
- 📘 [CLOUDFLARE_DEPLOYMENT.md](./CLOUDFLARE_DEPLOYMENT.md) - 完整部署指南
- 📙 [CLOUDFLARE_QUICK_REFERENCE.md](./CLOUDFLARE_QUICK_REFERENCE.md) - 快速參考

### 官方資源
- 🔗 [Cloudflare Pages 文檔](https://developers.cloudflare.com/pages/)
- 🔗 [Next.js on Cloudflare](https://developers.cloudflare.com/pages/framework-guides/nextjs/)
- 💬 [Cloudflare Community](https://community.cloudflare.com/)

### 技術支援
- Cloudflare Support（通過 Dashboard 提交工單）
- GitHub Issues（專案相關問題）

---

**文檔版本**: 1.0.0  
**最後更新**: 2026-01-12  
**適用於**: Next.js 15 + Cloudflare Pages

---

**🎊 準備好了嗎？現在就開始部署吧！**

**下一步**：前往 https://dash.cloudflare.com/ 並按照本指南操作！
