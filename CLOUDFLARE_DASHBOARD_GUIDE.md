# 🎯 Cloudflare Dashboard 更新設定 - 詳細步驟指南

## 📋 目標

更新 Cloudflare Pages 專案設定，將構建配置從 SSR 模式改為 Static Export 模式。

---

## 🚀 完整操作步驟

### 步驟 1️⃣：登入 Cloudflare Dashboard

1. **打開瀏覽器**，前往 Cloudflare：
   ```
   🔗 https://dash.cloudflare.com/
   ```

2. **登入您的帳號**
   - 輸入郵箱
   - 輸入密碼
   - 點擊 "Log in"

3. **如果有多個帳號**，選擇正確的帳號

---

### 步驟 2️⃣：進入 Workers & Pages

登入後，您會看到 Cloudflare 主控制台：

```
┌─────────────────────────────────────────────────┐
│  Cloudflare Dashboard                           │
│  ┌───────────────────────────────────────────┐  │
│  │  左側導航欄                                 │  │
│  │  ├─ 🏠 Home                               │  │
│  │  ├─ 📊 Analytics                          │  │
│  │  ├─ 🌐 Websites                           │  │
│  │  ├─ ⚡ Workers & Pages    ◀─────────────  │  │ 點擊這裡
│  │  ├─ 🔐 Zero Trust                         │  │
│  │  ├─ 📧 Email                              │  │
│  │  └─ ...                                   │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

1. **在左側導航欄**，找到並點擊 **"Workers & Pages"**
   - 圖標是閃電符號 ⚡
   - 可能在側邊欄的中間位置

2. **如果看不到**，可能需要：
   - 向下滾動側邊欄
   - 或點擊 "..." 展開更多選項

---

### 步驟 3️⃣：找到您的 Pages 專案

進入 Workers & Pages 頁面後：

```
┌─────────────────────────────────────────────────┐
│  Workers & Pages                                │
│  ┌─────────────┬─────────────┐                 │
│  │  Overview   │  Create     │                 │
│  └─────────────┴─────────────┘                 │
│                                                 │
│  📑 Your Pages projects                         │
│  ┌───────────────────────────────────────────┐ │
│  │  🌐 kanae-real-estate         ◀──────────  │ │ 點擊這個專案
│  │     https://kanae-real-estate.pages.dev   │ │
│  │     Last deployed: X minutes ago          │ │
│  │     Status: ❌ Failed (或其他狀態)         │ │
│  └───────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

1. **找到專案名稱**：`kanae-real-estate`
   - 通常在 "Your Pages projects" 區域
   - 顯示專案 URL 和最後部署時間

2. **點擊專案名稱** 或 **點擊整個卡片**
   - 會進入專案詳情頁面

---

### 步驟 4️⃣：進入 Settings（設定）

進入專案後，您會看到專案頁面：

```
┌─────────────────────────────────────────────────┐
│  kanae-real-estate                              │
│  ┌──────────────────────────────────────────┐  │
│  │  Deployments  │  Settings  │  Custom      │  │
│  │  ▲           │  ⚙️  ◀────  │  domains     │  │ 點擊 Settings
│  └──────────────────────────────────────────┘  │
│                                                 │
│  最近的部署記錄...                               │
└─────────────────────────────────────────────────┘
```

1. **在頂部標籤欄**，找到 **"Settings"** 標籤
   - 通常有齒輪圖標 ⚙️
   - 在 "Deployments" 旁邊

2. **點擊 "Settings"**
   - 會進入設定頁面

---

### 步驟 5️⃣：進入 Builds & deployments 設定

在 Settings 頁面，您會看到左側子導航：

```
┌─────────────────────────────────────────────────┐
│  Settings                                       │
│  ┌──────────────────┬────────────────────────┐ │
│  │  左側子選單        │  設定內容區域           │ │
│  │                 │                        │ │
│  │  General        │                        │ │
│  │  Builds &       │  ◀────────────────────  │ │ 點擊這個
│  │  deployments    │                        │ │
│  │  Functions      │                        │ │
│  │  Environment    │                        │ │
│  │  variables      │                        │ │
│  │  Custom domains │                        │ │
│  │  ...            │                        │ │
│  └─────────────────┴────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

1. **在左側子選單**，找到 **"Builds & deployments"**
   - 可能需要向下滾動
   - 有時顯示為 "Build configuration" 或類似名稱

2. **點擊 "Builds & deployments"**
   - 右側會顯示構建配置選項

---

### 步驟 6️⃣：編輯 Build configuration（構建配置）

在 Builds & deployments 頁面：

```
┌─────────────────────────────────────────────────┐
│  Builds & deployments                           │
│                                                 │
│  🔧 Build configuration                         │
│  ┌───────────────────────────────────────────┐ │
│  │  Framework preset                         │ │
│  │  Next.js (SSR)                    [Edit] │ │ ◀─ 點擊 Edit
│  │                                           │ │
│  │  Build command                            │ │
│  │  npm run build                    [Edit] │ │
│  │                                           │ │
│  │  Build output directory                   │ │
│  │  .next                            [Edit] │ │ ◀─ 點擊 Edit
│  │                                           │ │
│  │  Root directory (optional)                │ │
│  │  /                                [Edit] │ │
│  └───────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

**找到構建配置區域**，通常標題為：
- "Build configuration"
- "Production builds"
- 或類似名稱

---

### 步驟 7️⃣：修改 Framework preset（重要！）

1. **找到 "Framework preset"** 這一行

2. **點擊右側的 [Edit] 按鈕** 或 **點擊整行**

3. **會出現下拉選單**：

```
┌─────────────────────────────────────────┐
│  Framework preset                       │
│  ┌─────────────────────────────────────┤
│  │  🔍 Search frameworks...            │
│  ├─────────────────────────────────────┤
│  │  None                               │
│  │  Next.js (SSR)              ← 當前  │
│  │  Next.js (Static Export)    ← 選這個 │ ✅
│  │  React                              │
│  │  Vue                                │
│  │  Angular                            │
│  │  ...                                │
│  └─────────────────────────────────────┘
└─────────────────────────────────────────┘
```

4. **向下滾動列表**，找到 **"Next.js (Static Export)"**
   - 可能顯示為：
     - "Next.js (Static Export)"
     - "Next.js - Static HTML Export"
     - "Next.js (Static)"

5. **點擊 "Next.js (Static Export)"** 選項

6. **確認選擇**：
   ```
   ✅ Framework preset: Next.js (Static Export)
   ```

---

### 步驟 8️⃣：修改 Build output directory（重要！）

1. **找到 "Build output directory"** 這一行
   - 當前值應該是：`.next`

2. **點擊右側的 [Edit] 按鈕**

3. **會出現輸入框**：

```
┌─────────────────────────────────────────┐
│  Build output directory                 │
│  ┌─────────────────────────────────────┤
│  │  [.next                        ]    │ ← 當前值
│  └─────────────────────────────────────┤
│  這是構建生成的靜態檔案目錄               │
└─────────────────────────────────────────┘
```

4. **刪除當前的內容** `.next`

5. **輸入新的值** `out`

```
┌─────────────────────────────────────────┐
│  Build output directory                 │
│  ┌─────────────────────────────────────┤
│  │  [out                          ]    │ ← 新值
│  └─────────────────────────────────────┤
│  這是構建生成的靜態檔案目錄               │
└─────────────────────────────────────────┘
```

6. **確認輸入**：
   ```
   ✅ Build output directory: out
   ```

---

### 步驟 9️⃣：確認其他設定（保持不變）

**檢查以下設定，確保正確**：

```
┌─────────────────────────────────────────────────┐
│  ✅ 確認這些設定                                  │
├─────────────────────────────────────────────────┤
│  Production branch:     main            ✅       │
│  Build command:         npm run build   ✅       │
│  Root directory:        /               ✅       │
│  Node version:          20 (或自動)      ✅       │
└─────────────────────────────────────────────────┘
```

**這些設定應該保持不變**，不需要修改。

---

### 步驟 🔟：保存設定

完成所有修改後：

```
┌─────────────────────────────────────────────────┐
│  Build configuration                            │
│                                                 │
│  已修改的設定：                                   │
│  ✅ Framework preset: Next.js (Static Export)   │
│  ✅ Build output directory: out                 │
│                                                 │
│  ┌─────────────────────────────────────────┐   │
│  │                                         │   │
│  │  [Cancel]              [Save]  ◀─────── │   │ 點擊 Save
│  │                                         │   │
│  └─────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

1. **向下滾動到頁面底部**

2. **找到 [Save] 按鈕**
   - 通常是藍色或綠色
   - 可能顯示為 "Save" 或 "Save changes"

3. **點擊 [Save] 按鈕**

4. **等待確認**
   - 應該會顯示 "Settings saved" 或類似訊息
   - 頁面可能會刷新

---

### 步驟 1️⃣1️⃣：觸發重新部署

設定保存後，需要觸發重新部署。有兩種方式：

#### 方式 A：自動觸發（推薦）✅

**代碼已推送到 GitHub，應該會自動觸發新的構建**

1. **回到 "Deployments" 標籤**
   - 點擊頂部的 "Deployments"

2. **查看是否有新的部署正在進行**
   - 顯示 "Building..." 或 "In progress"

3. **如果有新部署**，跳到步驟 12

---

#### 方式 B：手動觸發

**如果沒有自動觸發，手動重新部署**：

```
┌─────────────────────────────────────────────────┐
│  Deployments                                    │
│                                                 │
│  Production                                     │
│  ┌───────────────────────────────────────────┐ │
│  │  ❌ Failed - 5 minutes ago                │ │
│  │  Branch: main                             │ │
│  │  Commit: 871b8f7                          │ │
│  │                                           │ │
│  │  [View details]    [Retry deployment] ◀─  │ │ 點擊這個
│  └───────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

1. **找到最近的部署記錄**
   - 通常在 "Production" 區域
   - 顯示為 Failed 或 Error

2. **點擊 [Retry deployment] 按鈕**
   - 或點擊 [...] 選單 > "Retry deployment"

3. **確認重新部署**
   - 如果有確認對話框，點擊 "Yes" 或 "Confirm"

---

### 步驟 1️⃣2️⃣：監控構建過程

重新部署開始後：

```
┌─────────────────────────────────────────────────┐
│  Deployments                                    │
│                                                 │
│  ⏳ Building...                                 │
│  ┌───────────────────────────────────────────┐ │
│  │  🔄 In progress - Just now                │ │
│  │  Branch: main                             │ │
│  │  Commit: 871b8f7                          │ │
│  │                                           │ │
│  │  [View build log]  ◀──────────────────────│ │ 點擊查看日誌
│  │  [Cancel deployment]                      │ │
│  └───────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

1. **點擊部署記錄**或點擊 **[View build log]**

2. **查看構建日誌**，應該會看到：

```
13:XX:XX  Initialized build environment
13:XX:XX  Cloning repository...
13:XX:XX  ✅ Success: Finished cloning repository files
13:XX:XX  Installing dependencies...
13:XX:XX  Running "npm install"
13:XX:XX  added 350 packages in 45s
13:XX:XX  Building application...
13:XX:XX  Running "npm run build"
13:XX:XX  > kanae-real-estate@0.1.0 build
13:XX:XX  > next build
13:XX:XX  ✓ Compiled successfully in 13.9s
13:XX:XX  ✓ Generating static pages (4/4)
13:XX:XX  ✓ Exporting (2/2)
13:XX:XX  ✅ Build succeeded!
13:XX:XX  Deploying to Cloudflare Pages...
13:XX:XX  ✅ Success! Your site is live!
```

3. **構建時間**：約 2-3 分鐘
   - Cloning: ~5 秒
   - Installing: ~45 秒
   - Building: ~40 秒
   - Deploying: ~10 秒

---

### 步驟 1️⃣3️⃣：確認部署成功

構建完成後：

```
┌─────────────────────────────────────────────────┐
│  Deployments                                    │
│                                                 │
│  Production                                     │
│  ┌───────────────────────────────────────────┐ │
│  │  ✅ Success - Just now                    │ │
│  │  Branch: main                             │ │
│  │  Commit: 871b8f7                          │ │
│  │  🌐 https://kanae-real-estate.pages.dev  │ │ ◀─ 點擊訪問
│  │                                           │ │
│  │  [View deployment]  [View build log]      │ │
│  └───────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

1. **確認狀態為 "Success"** ✅
   - 顯示綠色勾選圖標
   - 狀態文字為 "Success" 或 "Active"

2. **看到部署 URL**：
   ```
   https://kanae-real-estate.pages.dev
   ```

3. **點擊 URL** 或 **點擊 [View deployment]**
   - 會在新標籤頁打開您的網站

---

### 步驟 1️⃣4️⃣：驗證網站功能

網站打開後，執行完整測試：

```
✅ 網站驗證清單

基本功能：
├─ [ ] 網站可以正常訪問
├─ [ ] 首頁內容完整顯示
├─ [ ] 沒有顯示錯誤訊息
└─ [ ] 頁面載入速度快（< 2 秒）

導航功能：
├─ [ ] 導航欄正常顯示
├─ [ ] 滾動時背景顏色變化
├─ [ ] 語言切換按鈕可用
│   ├─ [ ] 切換到中文
│   ├─ [ ] 切換到英文
│   └─ [ ] 切換回日文
└─ [ ] 移動端選單可以打開/關閉

內容區域：
├─ [ ] Hero 區域（主視覺）正常
├─ [ ] Services 區域（4個服務卡片）
├─ [ ] Philosophy 區域（企業理念）
├─ [ ] Features 區域（6個特色）
├─ [ ] Stats 區域（統計數據）
├─ [ ] CTA 區域（行動呼籲）
└─ [ ] Footer（頁腳）完整顯示

響應式設計：
├─ [ ] 桌面版（>1024px）正常
├─ [ ] 平板版（768-1024px）正常
└─ [ ] 手機版（<768px）正常

技術檢查：
├─ [ ] 打開 Console (F12) 無錯誤
├─ [ ] 圖片全部正常載入
└─ [ ] 動畫效果流暢
```

---

### 步驟 1️⃣5️⃣：測試響應式設計

在瀏覽器中測試不同設備：

1. **按 F12** 打開開發者工具

2. **按 Ctrl+Shift+M** （或點擊設備圖標）切換到設備模式

3. **測試不同設備尺寸**：

```
📱 手機測試：
   iPhone SE (375x667)
   iPhone 12 Pro (390x844)
   
📱 平板測試：
   iPad (768x1024)
   iPad Pro (1024x1366)
   
💻 桌面測試：
   Desktop (1920x1080)
   Laptop (1366x768)
```

4. **確認**：
   - 佈局自動適應
   - 文字大小合適
   - 按鈕可以點擊
   - 圖片正確縮放

---

## 🎉 完成！

### 成功標誌

如果您看到以下情況，表示部署成功：

```
✅ Cloudflare Pages 顯示 "Success"
✅ 網站 URL 可以訪問
✅ 首頁內容完整顯示
✅ 所有功能正常運作
✅ 響應式設計正常
✅ 無 Console 錯誤
```

---

## 📸 設定前後對比

### 修改前（有問題）：
```yaml
Framework preset:       Next.js (SSR)
Build output directory: .next
結果:                   ❌ 構建失敗
```

### 修改後（已修復）：
```yaml
Framework preset:       Next.js (Static Export)
Build output directory: out
結果:                   ✅ 構建成功
```

---

## 🔄 自動部署已啟用

從現在開始，每次您推送代碼到 GitHub：

```
您在本地修改代碼
    ↓
git commit -m "更新內容"
    ↓
git push origin main
    ↓
GitHub 通知 Cloudflare
    ↓
Cloudflare 自動開始構建
    ↓
2-3 分鐘後自動部署完成
    ↓
網站自動更新
```

---

## 🆘 常見問題

### Q1: 找不到 "Next.js (Static Export)" 選項？

**解決方案**：
- 嘗試搜索 "Static"
- 或選擇包含 "Export" 字樣的選項
- 如果實在找不到，選擇 "None" 然後手動指定構建命令

---

### Q2: 保存後沒有自動觸發構建？

**解決方案**：
1. 手動點擊 "Retry deployment"
2. 或推送一個小更改到 GitHub：
   ```bash
   git commit --allow-empty -m "Trigger rebuild"
   git push origin main
   ```

---

### Q3: 構建仍然失敗？

**檢查清單**：
- [ ] Framework preset 是否改為 Static Export
- [ ] Output directory 是否改為 out
- [ ] Build command 是否為 npm run build
- [ ] 是否保存了設定

**如果都正確**，查看構建日誌尋找具體錯誤。

---

### Q4: 無法編輯設定（按鈕是灰色的）？

**可能原因**：
- 權限不足
- 正在進行中的構建

**解決方案**：
1. 等待當前構建完成
2. 確認您有管理員權限
3. 刷新頁面重試

---

## 📞 需要更多幫助？

### 參考文檔
- 📘 [CLOUDFLARE_BUILD_FIX_REPORT.md](./CLOUDFLARE_BUILD_FIX_REPORT.md)
- 📙 [DEPLOY_CONFIG_UPDATE.md](./DEPLOY_CONFIG_UPDATE.md)
- 📗 [CLOUDFLARE_DEPLOYMENT_WALKTHROUGH.md](./CLOUDFLARE_DEPLOYMENT_WALKTHROUGH.md)

### 官方資源
- 🔗 [Cloudflare Pages 文檔](https://developers.cloudflare.com/pages/)
- 💬 [Cloudflare Community](https://community.cloudflare.com/)

### 技術支援
- Cloudflare Support（通過 Dashboard 提交工單）

---

**指南版本**: 1.0.0  
**更新日期**: 2026-01-12  
**預計完成時間**: 10-15 分鐘  
**目標**: ✅ 網站成功部署到 Cloudflare Pages

---

**🎊 祝您部署順利！如有任何問題，隨時詢問！**
